类
   类基础
         *public成员提供类的接口，private成员提供实现细节
         *struct是成员默认为public的class
         *static成员 ？？

   默认拷贝
         *默认的拷贝语义是逐成员复制，如果对与类X这不是正确的选择，用户可以定义一个恰当的默
         认赋值运算符。

   访问控制
         *
